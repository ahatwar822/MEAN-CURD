<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page %></title>
    <style>
        body{
            background-color: rgba(127, 255, 212, 0.468);
            text-align: center;
           
        }
        h1{
            font-size: 50px;
            font-weight: bold;
        }
        a{
            color: red;
            background-color: white;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }
    </style>
</head>
<body>
    <h1> Student Data</h1>
    <h3><%= title %>   </h3>
    <marquee>
        <h4><a href = "/register">_ Student Register Here_</a></h4>
    </marquee>
    <% if (students!=""){%>
    <table border="4px" width="100%">
        <tr>
            <th>Roll No.</th>
            <th>Name</th>
            <th>Mail</th>
            <th>Age</th>
            <th>Oprations</th>
        </tr>
        <% students.forEach((students,index) => { %>
        <tr>
            <td><%= index+1 %></td>
            <td><%= students.name %></td>
            <td><%= students.mail %></td>
            <td><%= students.age %></td>
            <td><a href="/edit/<%= students._id %>">Update</a> | <a href = "/delete/<%= students._id %>">Delete</a></td>
            
        </tr>
        <% }) %>
    </table> <% } %>
</body>
</html>

